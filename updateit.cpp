#include<bits/stdc++.h>
using namespace std;
#define ll long long
/*void update(ll index,ll val,ll n,ll bit[])
{
	while(index<=n)
	{
		bit[index]=bit[index]+val;
		//index=index+(index&(-index));
	}
	return;
}
ll get(ll index,ll n,ll bit[])
{
	ll sum=0;
	while(index>=0)
	{
		sum=sum+bit[index];
		//index=index-(index&(-index));
	}
	return sum;
}*/
int main()
{
	ll t;
	scanf("%lld",&t);
	while(t--)
	{
	/*	ll bit[n+1];
		memset(bit,0,sizeof bit);
		ll l,r,val;
		while(u--)
		{
			sf(l);sf(r);sf(val);
			update(l+1,val,n,bit);
			update(r+2,-val,n,bit);
		}
		ll ith;
		sf(q);
		while(q--)
		{
			sf(ith);
			ll ans=get(ith+1,n,bit);
			printf("%lld\n",ans);
		}*/
	}
}
